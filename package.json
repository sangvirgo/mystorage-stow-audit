{
  "name": "mystorage-stow-audit",
  "version": "1.0.0",
  "description": "Black-box audit harness for stow.mystorage.vn (own account, one message at a time)",
  "private": true,
  "type": "module",
  "scripts": {
    "auth": "node src/auth.ts",
    "recon": "node src/recon.ts",
    "audit": "node src/audit.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.0.0",
    "typescript": "^5.9.0"
  }
}
